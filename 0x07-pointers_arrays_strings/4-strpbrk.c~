#include "main.h"

/**
 * *_strcpy - copies the string
 * @src: source
 * @dest: destination
 * Return: char
 */
char *_strpbrk(char *s, char *accept)
{
int i;

while (*s)
{
for (i = 0; accept[i]; i++)
{
if (*s == accept[i])
return (s);
}
s++
}
return ('\0');
}
