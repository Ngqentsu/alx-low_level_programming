#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the product of the two numbers.
 */
int mul(int a, int b)
{
return (product = a * b);
}
