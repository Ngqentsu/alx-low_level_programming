#include "main.h"

/**
 * _isdigit - checks for a digit
 * @c: the character to print
 * Return: 1 and 0 depending on the case.
 */
int _isdigit(int c)
{
if (c >= 0 && c <= 9)
{
return (1);
}
else
return (0);
}
