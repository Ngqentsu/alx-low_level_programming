#include "main.h"

/**
 * print_most_numbers - prints numbers
 * @n: the integer
 */
void print_most_numbers(void)
{
int n;
for (n = '0'; n <= '9'; i++)
{
if (!( n == '2' || n == '4'))
_putchar(n);
}
_putchar('\n');
}
