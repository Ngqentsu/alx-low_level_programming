#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * @c: the character
 * Return: 1 and 0 depending on the case alphabet
 */
int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
