#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *@n: the integer
 * Return:n if n is greater or equal to 0, -n if n is less than 0.
 */
int _abs(int n)
{
if (n  >=  0)
{
return(n);
}
else
return(-n);
}
