#include <stdio.h>
#include "main.h"

/**
 * main - a program that prints its name
 * @argc: argument count
 * @argv: the argument
 * Return: Always 0 (Success)
 */
int main(int argc, int *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
